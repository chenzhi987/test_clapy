import { memo, SVGProps } from 'react';

const Ellipse183Icon = (props: SVGProps<SVGSVGElement>) => (
  <svg preserveAspectRatio='none' viewBox='0 0 560 560' fill='none' xmlns='http://www.w3.org/2000/svg' {...props}>
    <circle
      cx={280}
      cy={280}
      r={204.974}
      transform='rotate(-30 280 280)'
      stroke='#28E1BE'
      strokeOpacity={0.5}
      strokeWidth={50}
    />
  </svg>
);

const Memo = memo(Ellipse183Icon);
export { Memo as Ellipse183Icon };
